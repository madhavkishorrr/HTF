from setuptools import setup

setup(
   name='hodmdExperiments',
   version='1.0',
   description='A Module for running experiments with HODMD for spatio-temporal data',
   author='Aadharsh Aadhithya',
   author_email='aadharshaadhithya@gmail.com',
   packages=['hodmdExperiments'],  #same as name
   install_requires=[], #external packages as dependencies
   scripts=[ ]
)